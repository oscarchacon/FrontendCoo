import { DataDelayDirective } from './data-delay.directive';

describe('DataDelayDirective', () => {
  it('should create an instance', () => {
    const directive = new DataDelayDirective();
    expect(directive).toBeTruthy();
  });
});
